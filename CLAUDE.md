# Matadragones Subsystems (AI driven)

A Foundry VTT module that runs PF2e subsystems with content generated by OpenAI.
Chases and Influence are implemented; Research and Infiltration are the obvious
next two.

## Ground rules

**Reuse before you add.** Every subsystem shares far more than it differs. Before
writing anything, look for the existing version of it:

| Concern | Lives in | Notes |
|---|---|---|
| Storage, hide/start/export/import/rename/push-to-players | `scripts/subsystems.js` | A registry keyed by subsystem. Add a subsystem here and the shared GM actions work for it immediately. |
| DC maths, party size/level, HTML↔prompt text, sorting, ids | `scripts/helpers.js` | |
| OpenAI text calls | `scripts/ai/openai.js` | Structured Outputs only. |
| Image generation, references, saving | `scripts/ai/image.js` | |
| Rolling, degrees, applying results | `scripts/rolls.js` | `chasePointsForDegree` is shared: the published values are identical in both subsystems. |
| Player→GM relay | `scripts/socket.js` | |
| Schema migration | `scripts/migrate.js` | |

If you find yourself writing a second copy of something, promote it instead.

**Keep the UX consistent across subsystems.** A GM should not have to learn one
set of habits on one tab and another on the next. Every subsystem gets the same
header — preview as player, push to players, hide, export, rename, delete — and
the same status-bar shape: round counter, the number that matters, then actions
including Start/Pause and Next Round. `check-templates.mjs` asserts this; if you
add a subsystem, add it to that assertion.

Shared buttons must carry `data-subsystem` and `data-event-id`. Subsystem-specific
ones may use their own attributes.

## Non-negotiables

These exist because getting them wrong produced real bugs:

- **The GM owns the DCs.** The model never emits a number. It picks a difficulty
  adjustment; `dcFromBase(baseDC, adjustment)` computes the DC. Models are
  unreliable at recalling the level-based DC table.
- **The module builds the inline-check syntax**, not the model. One bad token
  produces a dead link.
- **GM prose is verbatim.** Premises, NPC descriptions and goals the GM writes
  are stored exactly as typed and are never in the generation schema. The system
  prompt tells the model they are authoritative.
- **Mechanical values that scale with the party come from the published formula**,
  not the model. See `chasePointGoal`.
- **Credit what actually changed**, not the nominal value. A critical failure
  absorbed by the zero floor costs nobody a point.
- **World settings are GM-writable only.** Anything a player does that changes
  state goes over the socket and is applied by exactly one designated GM
  (`activeGM`), or two GMs double-count it.

## Testing

`npm test` — no Foundry needed, the harnesses stub the globals.

- `check-imports.mjs` — **runs first, and for good reason.** A dangling import
  breaks the entire ES module graph and Foundry reports only an inactive module
  with no error. It also checks every registered ApplicationV2 action has a
  handler; a registered-but-undefined private method is a SyntaxError in the
  class body, with the same silent symptom.
- `check-logic.mjs` — DC and goal maths, prompt/route text, socket targeting,
  Structured Outputs strict-mode invariants.
- `check-openai.mjs` — request shape and every error path.
- `check-templates.mjs` — renders every template as GM and as player, asserts no
  GM content leaks, asserts action buttons carry the ids their handlers read, and
  asserts subsystem UX parity.

When a test fails, **check the assertion before the code** — several times the
fixture was wrong, not the implementation. Say so plainly if that turns out to be
the case.

## Foundry gotchas, verified in a live v13.351 world

- `ApplicationV2` has **no** drag-drop support: no `dragDrop` option, no
  callbacks. Wire listeners by hand in `_onRender`. `dragleave` fires for child
  elements, so count nesting depth.
- `ApplicationV2` gives no way to choose which `PARTS` render. Use named partials
  registered with `foundry.applications.handlebars.loadTemplates({name: path})`
  at `setup`.
- Actions fire on **click only**. A `<select>` will not trigger one; use buttons
  or read the select when a button is clicked.
- Block parameters (`{{#each xs as |x|}}`) are lexically scoped — write `{{x.id}}`
  in nested blocks, never `{{../x.id}}`, which silently resolves to nothing.
- Settings forms submit an empty number input as `NaN`, which no nullable
  `NumberField` can validate. Use `String` and parse at point of use.
- `innerText` returns `''` for collapsed `<details>`; use `textContent`.
- Module `title` is cached at server start — a rename needs a Foundry restart.
- PF2e: `actor.getStatistic(slug).roll({dc})` returns a `CheckRoll` carrying
  `degreeOfSuccess` (0–3). Lore slugs end in `-lore`.
- The images endpoint rejects SVG, and Foundry ships SVG icons — rasterise
  references to PNG first.
- A drop zone must **contain** the text inviting the drop. An empty roster whose
  hint sits outside it is a thin header strip, and dropping is silently
  impossible. Give zones a `min-height` too.
- When you widen a method's signature, check the `api` wrappers in `module.js`
  and the socket handlers forward the new argument.

## Local Foundry

`.local/` is gitignored and holds a Foundry install plus its data, with the
module symlinked in. If the server stops:

```
node .local/app/main.js --dataPath=.local/data --port=30000 --noupnp
```

Then relaunch the world from the setup screen. Never commit `.local/` — the world
database contains the OpenAI API key.

## Adding a subsystem

1. `scripts/data/<name>.js` — the DataModel pair.
2. Register the setting in `scripts/settings.js` and add store accessors to
   `helpers.js`.
3. Add an entry to `SUBSYSTEMS` in `scripts/subsystems.js`. The shared GM actions
   now work.
4. `scripts/ai/<name>.js` — strict schema, prompt, and payload→stored mapping.
   Collect from the GM whatever the model cannot invent.
5. A generate dialog and a view partial.
6. Add a tab, and add the subsystem to the parity assertion in
   `check-templates.mjs`.
